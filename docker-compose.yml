services:
  web:
    build: .
    ports:
      - "5050:5000"
    environment:
      PORT: "5000"
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
    volumes:
      - ./uploads:/app/uploads
      - ./transcripts:/app/transcripts
      - ./outputs:/app/outputs
    restart: unless-stopped
